/*global define, console */

define(['angular'], function (angular) {
    "use strict";

    var factory = function (
    ) {

        return {

        }

    };

    factory.$inject = [
    ];
    return factory;
});